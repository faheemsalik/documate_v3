/**
 * FrontendSupport API origin.
 * Dev: call the API directly (CORS allowed for localhost:4202 in Development).
 * Same-origin proxy (`proxy.conf.json` with `/api/**`) remains available if you set this to `''`.
 */
export const API_BASE_URL = 'http://localhost:5172';
